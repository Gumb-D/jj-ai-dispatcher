# Phase 5.7B Dispatch Tool Gated Smoke

## Objective

Validate the first controlled `dispatcher_dispatch` execution through the existing MCP stdio server and Local Dispatcher Bridge.

This was a smoke validation only. It did not add MCP tools, expand the API surface, change bridge behavior, add remote access, or alter the existing safety boundary.

## Commands Executed

```powershell
git status --short
git rev-parse --short HEAD
git log -1 --oneline
npm install
npm run build
.\scripts\bridge-status.ps1
npm run mcp:smoke
<Node MCP stdio client invoking dispatcher_dispatch once>
.\scripts\bridge-status.ps1
.\scripts\bridge-latest.ps1
git status --short
Get-Content -Raw .\docs\dispatch-tool-smoke.tmp.md
git log -3 --oneline
Get-Content -Raw .\dispatcher\runs\20260526-205850-fe7136fb\result.json
Get-Content -Raw .\dispatcher\runs\20260526-205850-fe7136fb\summary.md
npm run mcp:smoke
git diff --check
git status --short
```

## Dispatch Payload Used

```json
{
  "repo": "self",
  "worker": "codex",
  "task": "Create or update docs/dispatch-tool-smoke.tmp.md with exactly this validation artifact content:\n\n# Dispatch Tool Smoke\n\nGenerated by dispatcher_dispatch gated smoke validation.\n\nTimestamp:\n2026-05-26T20:58:47.0862649+08:00\n\nPurpose:\nValidate controlled dispatcher dispatch execution.\n\nNo architecture changes.\nNo new features.\n\nConstraints:\n- Documentation-only change.\n- Do not change code.\n- Do not change config.\n- Do not expose secrets.\n- Do not start any follow-up dispatch.",
  "commitMessage": "test: gated dispatcher dispatch smoke",
  "scope": [
    "docs/dispatch-tool-smoke.tmp.md"
  ],
  "blocked": [
    "dispatcher/",
    "scripts/",
    "mcp/",
    "package.json",
    "package-lock.json",
    "dispatcher/config.local.json"
  ],
  "validation": [
    "git diff --check",
    "git status --short"
  ],
  "expectedOutput": [
    "One documentation-only validation artifact at docs/dispatch-tool-smoke.tmp.md.",
    "No code, config, bridge, MCP tool, listener, or remote access changes."
  ]
}
```

## Observed MCP Behavior

- MCP stdio client listed exactly four tools:
  - `dispatcher_status`
  - `dispatcher_dispatch`
  - `dispatcher_latest_result`
  - `dispatcher_get_run`
- `dispatcher_dispatch` accepted the explicit payload.
- Dispatch response:
  - `accepted: true`
  - `status: running`
  - `worker: codex`
  - `taskState: running`
- No token was printed in MCP output.
- No follow-up dispatch was started.

## Bridge Behavior

- Pre-dispatch bridge status was healthy:
  - `status: ok`
  - `bridgeEnabled: True`
  - `taskState: idle`
  - `defaultWorker: codex`
  - `autoPush: False`
- During dispatch, bridge status moved to `taskState: running`.
- After polling, bridge returned to `taskState: idle`.
- Latest run was available through `.\scripts\bridge-latest.ps1`.

## Dispatcher Behavior

Latest run result:

- `taskId`: `20260526-205850-fe7136fb`
- `status`: `success`
- `worker`: `codex`
- `filesChanged`: `docs/dispatch-tool-smoke.tmp.md`
- `commit`: `85d5b39`
- `commitMessage`: `test: gated dispatcher dispatch smoke`
- `pushed`: `False`
- `workingTreeClean`: `True`
- `summary`: `Codex worker changes committed by dispatcher.`
- `needsReview`: `False`

The result contract and summary were produced under:

- `dispatcher/runs/20260526-205850-fe7136fb/result.json`
- `dispatcher/runs/20260526-205850-fe7136fb/summary.md`
- `dispatcher/runs/20260526-205850-fe7136fb/codex-output.log`
- `dispatcher/runs/20260526-205850-fe7136fb/codex-error.log`
- `dispatcher/runs/20260526-205850-fe7136fb/git-diff.patch`

## Resulting Artifact / Commit Behavior

The dispatch created and committed:

- `docs/dispatch-tool-smoke.tmp.md`

Dispatcher-created commit:

```text
85d5b39 test: gated dispatcher dispatch smoke
```

The artifact content documents the timestamped smoke validation and includes the MCP safety envelope appended by the dispatcher task formatting.

No push was performed.

## Safety Observations

- MCP remained stdio-only.
- Bridge remained localhost-only and token-protected.
- The approved MCP tool list stayed at exactly four tools.
- The post-dispatch MCP smoke harness passed.
- No forbidden tool names were observed:
  - `arbitrary_shell`
  - `arbitrary_file_read`
  - `arbitrary_file_write`
  - `delete`
  - `push`
  - `tunnel_enable`
  - `remote_exec`
  - `vscode_ui_control`
  - `credential_read`
  - `config_write`
- No arbitrary shell, arbitrary file read/write, direct Git MCP tool, tunnel, remote bridge, public listener, or editor control was added.
- Dispatch remained explicit and operator-gated for this run.

## Blocking / Non-Blocking Findings

| Finding | Severity | Blocking? | Notes |
| --- | --- | --- | --- |
| `dispatcher_dispatch` successfully executed one docs-only task and committed the artifact. | Info | No | This is the expected smoke result. |
| The generated artifact includes the MCP safety fields appended to the task envelope. | Info | No | This confirms the existing dispatch adapter passed the safety envelope to the worker. |
| No push occurred. | Info | No | `autoPush` remained false and result contract reported `pushed: false`. |

## Validation Summary

- `npm install`: PASS
- `npm run build`: PASS
- `npm run mcp:smoke` before dispatch: PASS
- `.\scripts\bridge-status.ps1`: PASS
- One gated `dispatcher_dispatch`: PASS
- Bridge returned to idle: PASS
- Latest run/result contract available: PASS
- `npm run mcp:smoke` after dispatch: PASS

## Recommendation for Phase 5.8

Proceed to Phase 5.8 only if the operator accepts the dispatcher-created commit `85d5b39`.

Keep the MCP boundary unchanged for the next phase: stdio-only, localhost bridge only, token protection, exactly four approved tools, explicit operator-gated dispatch, and review after every dispatch result.
